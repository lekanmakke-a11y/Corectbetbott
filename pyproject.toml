[project]
name = "corectbet-bot"
version = "1.0.0"
description = "CorectBet Telegram Bot"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.railway]
builder = "RAILPACK"
